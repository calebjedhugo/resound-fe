// Mesh colors per entity type — shared by placed meshes (EntityPlacer) and
// the placement ghost (GhostPreview) so the preview always matches the result.
const ENTITY_COLORS = {
  player: 0xff4444,
  creature: 0xffaa00,
  gate: 0x4488ff,
  fountain: 0x44ddff,
  wall: 0x808080,
  ramp: 0x88ff88,
};

export default ENTITY_COLORS;
