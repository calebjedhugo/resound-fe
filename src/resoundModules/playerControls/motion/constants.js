const INCREMENT_DENOMINATOR = 1400000;
const CENTER_RANGE_PERC_X = 0.4;
const CENTER_RANGE_PERC_Y = 1;
// The constant motion should start before the mouse actually reaches the end of the range. CENTER_MOTION_START_OFFSET sets that value (in px).
const CENTER_MOTION_START_OFFSET = 150;

export {
  INCREMENT_DENOMINATOR,
  CENTER_RANGE_PERC_X,
  CENTER_RANGE_PERC_Y,
  CENTER_MOTION_START_OFFSET,
};
